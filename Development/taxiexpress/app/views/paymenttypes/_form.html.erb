<%= simple_form_for @paymenttype, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= error_span(@paymenttype[:name]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                paymenttypes_path, :class => 'btn btn-default' %>
<% end %>
